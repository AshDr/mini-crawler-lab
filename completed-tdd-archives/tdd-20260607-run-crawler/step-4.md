# Step 4 - Implement to Make Tests Pass

## Implementations Completed

- FR-1: Run A JS Fallback Mini Crawler - `docs/scenario/run-crawler.md` - Implementation in `run_crawler.py`, with default `configs/sites.yaml` and `seed_urls.txt`.

The crawler now loads inputs, schedules URLs, fetches and parses pages, learns profiles, and writes metrics.
